import { Component, Output, Input, EventEmitter } from '@angular/core';

@Component({
  selector: 'app-dialog-content',
  template: `<p> This is some dialog content <p>`,
  styles: []
})
export class DialogContentComponent  {
}